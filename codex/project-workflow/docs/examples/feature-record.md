# 合成示例：会话过期提示

> 状态:已确认。仅作写法示例，没有真实执行或交付结果。

## 需要解决的问题

用户会话过期后不知道下一步该做什么。沿用现有会话和登录流程，显示过期提示及重新登录入口。不新增 API、不改变会话生命周期、不做自动刷新。

## 方案与依据

示例假设：现有会话接口已经能返回过期状态，因此先复用它。真实项目必须查看自己的接口证据，不能把本例当作验证。确认的交互是提示原因并提供已有登录入口；不需要新的恢复状态或后台服务。

## 验收例子

| 场景 | 预期 |
|---|---|
| 会话过期 | 显示提示及重新登录入口 |
| 会话未过期 | 不显示过期提示 |
| 点击重新登录 | 进入已有登录流程，不自动重试原操作 |

实现时扩展现有会话页面测试，并用一次可复核的浏览器检查验证入口。测试位置和运行结果在真实项目中补入，不凭空指定路径或宣称通过。

## 实施记录

目前未实现、未测试。下一步先核对现有状态来源，再实现提示并验证。若实际接口没有所需状态，先说明影响、与用户确认是否调整范围，再局部修订；普通渲染细节无需重新确认。
