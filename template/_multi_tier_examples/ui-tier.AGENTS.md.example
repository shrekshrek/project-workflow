# `{{TIER_NAME}}` tier — AGENTS.md(差量于根 AGENTS.md)

> 本 tier 跟根 AGENTS.md 默认**不同**的部分写在这里。
> 重复根的内容会漂移 —— 见根 AGENTS.md / [`workflow.md §1.3 反模式`](https://github.com/shrekshrek/project-workflow/blob/main/docs/workflow.md#13-agentsmd-的内容标准)。

<!--
P0 时根据栈填齐 placeholder。
本文件目标 < 100 行,长尾放 .claude/rules/ 或 docs/。
-->

## Commands(本 tier 特有)

- 起开发:`{{TIER_DEV_COMMAND}}`
- 跑测试:`{{TIER_TEST_COMMAND}}`
- Lint:`{{TIER_LINT_COMMAND}}`
- Build:`{{TIER_BUILD_COMMAND}}`
- 组件测试(可选):`{{TIER_COMPONENT_TEST_COMMAND}}`

## Testing(本 tier 特有)

- 单元 / 组件:`{{TIER_TEST_FRAMEWORK}}`(如 Vitest + @vue/test-utils 或 Jest + React Testing Library)
- E2E:`{{TIER_E2E_FRAMEWORK}}`(如 Playwright / Cypress)
- 测试文件位置:`{{TIER_TEST_LOCATION}}`
- 覆盖率:继承根

## Module Structure

`{{TIER_SRC_DIR}}/modules/<bounded-context>/` 的典型组织:

- `views/` 或 `pages/` — 路由级页面
- `components/` — 组件
- `composables/` 或 `hooks/` — 业务逻辑封装
- `stores/` — state(per bounded-context)
- `services/` — API client(对应后端 `<tier>/<same-bounded-context>`)

**职责分离**:
- {{TIER_LAYER_SEPARATION_1}}(如 "组件不直接调 API,通过 composable")
- {{TIER_LAYER_SEPARATION_2}}(如 "store 不操作 DOM")

## Code Style(本 tier 特有)

**Source of truth**:
- `.claude/rules/code-style.md`(继承根)
- `{{TIER_LINT_CONFIG}}`(如 `.eslintrc.cjs` / `eslint.config.js`)

**本 tier 特殊**(不重复根):
- {{TIER_STYLE_HIGHLIGHT_1}}(如 "Vue 3 Composition API only,禁止 Options API")
- {{TIER_STYLE_HIGHLIGHT_2}}(如 "`<script setup>` 必用,不写 export default")
- {{TIER_STYLE_HIGHLIGHT_3}}(如 "TypeScript strict mode + noImplicitAny")

## UI 组件 + 样式

### UI 库:{{TIER_UI_LIB}}

**优先用 {{TIER_UI_LIB}} 组件,不自己造**:

{{TIER_UI_USAGE_RULES}}

<!-- 例(Nuxt UI):
  - 表单:UForm + UFormGroup + UInput / USelect
  - 反馈:UAlert / UNotification / UModal
  - 列表:UTable(支持 server-side pagination/sort)
  - 特殊样式才用 Tailwind class,**不另起 UI 库**

  例(shadcn-vue):
  - 优先 shadcn 组件 + Tailwind
  - 不混合 Element Plus / Ant Design
-->

### 样式方案:{{TIER_STYLING}}

{{TIER_STYLING_RULES}}

<!-- 例(Tailwind):
  - 优先 utility class,组件级用 @apply 提取重复
  - 全局样式只放 reset / 字体 / CSS variables
  - 颜色 / 间距 / 圆角统一用 theme tokens,不硬编码
-->

## State Management

### 方案:{{TIER_STATE_LIB}}

{{TIER_STATE_RULES}}

<!-- 例(Pinia):
  - `defineStore('<name>', () => ...)` setup style only
  - Composables 命名:`useXxx.ts`(useApi / useAuth / useTable)
  - store 文件位置:`stores/<name>.ts`
  - 异步逻辑放 store 的 action,不放组件

  例(Redux Toolkit):
  - `createSlice` for state + reducers
  - `createAsyncThunk` for async
  - selectors 在 `selectors/` 单独文件
-->

## 框架特殊约定(**label-only**,detail 走 `.claude/rules/<fw>.md`)

> ⚠️ 每条只给短标签(单行 ≤ 80 字符)。Detail 严禁重复 ── 违反 → 两边漂移(workflow §1.3 反模式 / 本仓 F-52)。

### {{TIER_FRAMEWORK}}(critical labels,≤ 5 个)

{{TIER_FRAMEWORK_CRITICAL_LABELS}}

<!-- 例(Vue 3):Composition API + <script setup> / defineProps generic / ref vs reactive / useXxx / 路由 lazy + guard 走 router 配置
     例(React 19):函数组件 + hooks / Rules of Hooks / state 选型不混 / useEffect deps 显式 / list key 用 stable id -->


## Boundaries(本 tier 特有,继承根)

- ✅ Always:加组件 / 改 component 内部 / 写 composable
- ⚠️ Ask first:
  - 引入新 UI 库依赖(混库会风格漂)
  - 改全局 state schema
  - 改路由结构
  - 加新 npm 依赖
- 🚫 Never:同根
