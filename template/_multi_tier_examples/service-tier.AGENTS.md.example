# `{{TIER_NAME}}` tier — AGENTS.md(差量于根 AGENTS.md)

> 本 tier 跟根 AGENTS.md 默认**不同**的部分写在这里。
> 重复根的内容会漂移 —— 见根 AGENTS.md / [`workflow.md §1.3 反模式`](https://github.com/shrekshrek/project-workflow/blob/main/docs/workflow.md#13-agentsmd-的内容标准)。

<!--
仅在 project-personalize 从仓库证据确认持久 tier 差异时替换 placeholder。
保持简洁；长尾放 docs/ 或当前宿主已采用的 scoped convention files。
-->

## Source Layout

> Single source of truth for src 路径。其他节(Commands / Testing / Module
> Structure)统一引用 `{{TIER_SRC_DIR}}`,改 layout 只改本节。

- 包根:`{{TIER_SRC_DIR}}/`
- 入口:`{{TIER_ENTRY_POINT}}`({{TIER_FRAMEWORK}} app 实例 / main entry)
- 测试镜像:`{{TIER_TEST_DIR}}/` ↔ `{{TIER_SRC_DIR}}/`

## Commands(本 tier 特有)

- 起服务:`{{TIER_DEV_COMMAND}}`
- 跑测试:`{{TIER_TEST_COMMAND}}`
- Lint:`{{TIER_LINT_COMMAND}}`
- DB migration:`{{TIER_MIGRATION_COMMAND}}`
- 任务队列(可选):`{{TIER_QUEUE_COMMAND}}`

## Testing(本 tier 特有)

- 框架:`{{TIER_TEST_FRAMEWORK}}`(如 Python pytest + httpx;Go std testing + testify + httptest;Java JUnit + MockMvc)
- 测试文件位置:`{{TIER_TEST_DIR}}/` 镜像 `{{TIER_SRC_DIR}}/`
- 按实际风险覆盖 happy、边界和错误路径；不设通用固定数量

## Module Structure

仅当仓库已有这种模式时，`{{TIER_SRC_DIR}}/<bounded-context>/` 可按下列职责分层；具体文件名以实际代码为准:

- **HTTP 路由层**(Python `router.py` / Go `handler.go` / Java `*Controller.java` / etc.)
- **业务逻辑**(Python `service.py` / Go `service.go` / Java `*Service.java`)
- **数据访问**(Python `repository.py` / Go `repository.go` / Java `*Repository.java`)— 可选,小项目可让 service 直连 ORM
- **输入输出 schema / DTO**(Python `schemas.py` Pydantic / Go `dto.go` struct tag / TypeScript `dto.ts` Zod / Java `*Dto.java` Bean Validation)
- **数据模型**(Python `models.py` ORM / Go `models.go` struct / Java `*Entity.java` JPA)

**职责分离**:
- {{TIER_LAYER_SEPARATION_1}}(如 "service 层不依赖 {{TIER_FRAMEWORK}}",可单元测试 + 复用)
- {{TIER_LAYER_SEPARATION_2}}(如 "repository 只返 model,不返 schema/DTO",避免穿透)

## Code Style(本 tier 特有)

**Source of truth**:
- 根/上级 `AGENTS.md`(继承)
- 当前宿主已采用的 path-scoped convention file(可选)
- `{{TIER_LINT_CONFIG}}`(如 Python `pyproject.toml` `[tool.ruff]`;Go `.golangci.yml`;Java `checkstyle.xml`)

**本 tier 特殊**(不重复根,据栈选项目级"真特殊"点 1-3 条):
- {{TIER_STYLE_HIGHLIGHT_1}}(如 Python "严格 type hints + `from __future__ import annotations`";Go "显式 `err` return,禁 panic 在 lib code";Java "Optional 优先于 null")
- {{TIER_STYLE_HIGHLIGHT_2}}(如 Python "Pydantic v2 strict mode";Go "Context 必走第一个参数";Java "Record 优先于 class for DTO")
- {{TIER_STYLE_HIGHLIGHT_3}}(如 Python "SQLAlchemy 2.0 `select()`";Go "sqlx struct tag,禁 raw SQL 拼接";Java "JPA `@Transactional` 边界明确")

## 框架特殊约定(**label-only**,detail 走 docs/ 或当前宿主已有的 scoped convention file)

> 每条只给短标签。Detail 不在两处重复，避免漂移(workflow §1.3 反模式)。

### {{TIER_FRAMEWORK}}(仅保留有仓库证据的 critical labels)

{{TIER_FRAMEWORK_CRITICAL_LABELS}}

<!-- 例(FastAPI):APIRouter 分组 / Depends DI / HTTPException 集中处理 / async 内禁 sync I/O / 输出返 Pydantic schema -->

### {{TIER_ORM}}(如适用)

{{TIER_ORM_CRITICAL_LABELS}}

### {{TIER_QUEUE}}(可选)

{{TIER_QUEUE_CRITICAL_LABELS}}

## Boundaries(本 tier 特有,继承根)

- ✅ Always:加测试 / 改模块内部 / 写 schema
- ⚠️ Ask first:
  - 加 DB migration(具体工具按栈:Python Alembic / Go golang-migrate / Java Flyway / 等)
  - 改公开 API 契约
  - 改 {{TIER_QUEUE}} 配置(broker / serializer / 并发数)
  - 加新外部依赖(库 / API)
- 🚫 Never:同根
