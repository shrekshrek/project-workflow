# `{{TIER_NAME}}` tier — AGENTS.md(差量于根 AGENTS.md)

> 本 tier 跟根 AGENTS.md 默认**不同**的部分写在这里。
> 重复根的内容会漂移 —— 见根 AGENTS.md / [`workflow.md §1.3 反模式`](https://github.com/shrekshrek/project-workflow/blob/main/docs/workflow.md#13-agentsmd-的内容标准)。

<!--
P0 时根据栈填齐 placeholder。
本文件目标 < 100 行,长尾放 .claude/rules/ 或 docs/。
-->

## Source Layout

> Single source of truth for src 路径。其他节(Commands / Testing / Module
> Structure)统一引用 `{{TIER_SRC_DIR}}`,改 layout 只改本节。

- 包根:`{{TIER_SRC_DIR}}/`
- 入口:`{{TIER_ENTRY_POINT}}`({{TIER_FRAMEWORK}} app 实例 / main entry)
- 测试镜像:`{{TIER_TEST_DIR}}/` ↔ `{{TIER_SRC_DIR}}/`

## Commands(本 tier 特有)

- 起服务:`{{TIER_DEV_COMMAND}}`
- 跑测试:`{{TIER_TEST_COMMAND}}`
- Lint:`{{TIER_LINT_COMMAND}}`
- DB migration:`{{TIER_MIGRATION_COMMAND}}`
- 任务队列(可选):`{{TIER_QUEUE_COMMAND}}`

## Testing(本 tier 特有)

- 框架:`{{TIER_TEST_FRAMEWORK}}`(如 pytest + httpx + pytest-asyncio)
- 测试文件位置:`{{TIER_TEST_DIR}}/` 镜像 `{{TIER_SRC_DIR}}/`
- 每个 endpoint 至少:happy + 1 边界 + 1 错误路径(401 / 404 / 422)

## Module Structure

`{{TIER_SRC_DIR}}/<bounded-context>/` 五件套(每个领域模块):

- `router.py` — HTTP 路由层(或框架等价物)
- `service.py` — 业务逻辑
- `repository.py` — 数据访问
- `schemas.py` — Pydantic / Zod / etc. 输入输出 schema
- `models.py` — ORM model

**职责分离**:
- {{TIER_LAYER_SEPARATION_1}}(如 "service.py 不依赖 {{TIER_FRAMEWORK}}")
- {{TIER_LAYER_SEPARATION_2}}(如 "repository.py 只返 model,不返 schema")

## Code Style(本 tier 特有)

**Source of truth**:
- `.claude/rules/code-style.md`(继承根)
- `{{TIER_LINT_CONFIG}}`(如 `pyproject.toml` ruff 节)

**本 tier 特殊**(不重复根):
- {{TIER_STYLE_HIGHLIGHT_1}}(如 "严格 type hints + `from __future__ import annotations`")
- {{TIER_STYLE_HIGHLIGHT_2}}(如 "Pydantic v2 strict mode,参考 `docs/gotchas.md` #3")
- {{TIER_STYLE_HIGHLIGHT_3}}(如 "SQLAlchemy 2.0 风格:用 `select()`,禁 `Query.filter`")

## 框架特殊约定(critical only,详规则 split 到 `.claude/rules/`)

### {{TIER_FRAMEWORK}}(critical rules,≤ 5 条)

{{TIER_FRAMEWORK_CRITICAL_RULES}}

**详规则**:见 `.claude/rules/<framework>.md`(P0 时若 `_examples/<framework>.example.md` 存在,skill 已自动 `cp` + 适配 globs)。

### {{TIER_ORM}}(critical rules,如适用,≤ 3 条)

{{TIER_ORM_CRITICAL_RULES}}

### {{TIER_QUEUE}}(critical rules,可选,≤ 3 条)

{{TIER_QUEUE_CRITICAL_RULES}}

## Boundaries(本 tier 特有,继承根)

- ✅ Always:加测试 / 改模块内部 / 写 schema
- ⚠️ Ask first:
  - 加 alembic migration(或对应 ORM migration)
  - 改公开 API 契约
  - 改 {{TIER_QUEUE}} 配置(broker / serializer / 并发数)
  - 加新外部依赖(库 / API)
- 🚫 Never:同根
