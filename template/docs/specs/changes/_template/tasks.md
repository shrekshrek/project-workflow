# <NNN> <slug> — Work notes

> 可选：只有清单较长、需要独立交接时才拆出。接受规则和交付收据留在 spec.md。

<!-- 按实际依赖记录可检查的工作，不要求提前发现全部任务，也不按文件/层级凑分组。
     已有测试能证明的行为不重复建一层测试；新任务不等于新的已接受需求。
     有实质方向或边界变化时主动沟通，确认后更新受影响内容，再继续。
     READY、Proof Bundle、状态写入和归档资格是 endpoint/lifecycle 输出，不写成 checkbox。 -->
