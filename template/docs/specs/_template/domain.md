# {{area}} — 产品域现状

> 最后核对:{{TODAY}}(as of {{source}})

> **E 类 domain doc**:回答该域**现在**怎么工作。变更意图写 `docs/specs/changes/<NNN>-<slug>/` 的 Delta,不要在本文件堆演进史。

## 当前行为

- {{TODO:该域用户可见能力 / API / 数据契约的一页摘要}}

## 关键约束

- {{TODO:该域持久规则(默认值 / 上限 / 状态机),有硬数字写数字}}

## 相关 ADR

- (无则删本节) [`docs/adr/`](../adr/) 链到解释「为什么」的决策,不在此复述论证
