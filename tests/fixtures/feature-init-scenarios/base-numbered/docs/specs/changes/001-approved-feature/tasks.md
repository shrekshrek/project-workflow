# 001 approved-feature — Tasks

## 1. 任务清单

### Profile 会话列表展示

- [ ] 使用现有 auth 契约实现会话列表。
- [ ] Focused check: 运行现有 profile/session 展示测试并映射 spec §4。

### Verification

N/A(all spec §4 obligations already covered by focused checks)

## 2. 实施记录

- 无。

## Proof Bundle

- Verdict:
- Change:`git=[base=<commit SHA>; reviewed=<commit SHA>; dirty=no]` 或 `git=[base=<commit SHA>; reviewed=worktree; dirty=yes]`; `endpoint-outputs=[tasks.md receipt, READY spec status when written]`
- Checks:
- Reviews:`L2=<completed + verdict/baseline/exceptions | N/A/not-run/invalidated + reason>; L3=<same shape>`
- Current truth:
