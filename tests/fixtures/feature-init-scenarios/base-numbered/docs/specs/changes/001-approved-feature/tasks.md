# 001 approved-feature — Tasks

## 1. 任务清单

### `S1` Profile 会话列表展示

- [ ] 使用现有 auth 契约实现会话列表。
- [ ] Focused L1: 运行现有 profile/session 展示测试并映射 spec §4。

### Verification

N/A(all spec §4 obligations mapped in slices)

## 2. 实施记录

- 无。

## Proof Bundle

- Verdict:
- Change:`git=[base=<commit SHA>; reviewed=<commit SHA>; dirty=no]` 或 `git=[base=<commit SHA>; reviewed=worktree; dirty=yes]`; `endpoint-outputs=[tasks.md receipt, READY spec status when written]`
- Checks:
- Review execution:`L2=<state=completed|not-run(completion preflight)|not-run(L1 prerequisite)|not-run(review-package incomplete)|invalidated(review-input drift)>; L3=<same shape>`
- L2:
- L3:
- Current truth:
