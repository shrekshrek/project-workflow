# 项目工程陷阱

> 这里只记录**本项目已经验证过、可能复发**的工程故障。新项目保持为空;不要预填其他技术栈的经验。

每条建议包含:

- 症状和最小复现
- 根因证据
- 已验证修法与验证命令
- 适用路径、版本或环境边界

**出口纪律**(写入新条目时顺手通读执行,ledger 只进不出必然腐化):

- **升格即删条**:某条坑反复触发、值得强制 → 把正例提炼进 `AGENTS.md` / path-scoped rules,本文件删该条(git history 即归档)
- **前提失效即删条**:条目依赖的栈 / 基建已移除(如弃用 Docker、换测试框架)→ 删
- 条目明显变多(如 > ~10 条)时检查是否该升格或清理

## 已确认陷阱

- (none yet)
